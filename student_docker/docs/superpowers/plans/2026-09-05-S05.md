# S05 Weighted Soup Search

User-approved design: search convex weights in order r016, r015, r012 with no retraining. Final objective: maximize RUS_o subject to full score_model.py AUS >= 0.995. This is a bounded local search, not a proof of the global optimum or private improvement.

Initial candidates: exact equal thirds, five user ratios (0.5/0.3/0.2, 0.5/0.2/0.3, 0.4/0.4/0.2, 0.4/0.2/0.4, 0.6/0.2/0.2), and three single-model endpoints. Then six directed 0.05 transfers around each of the best two screened candidates, followed by six 0.025 transfers around the best candidate; remove duplicate and invalid simplex points. At most 27 fast full-validation evaluations.

Fast ranking uses AUS >= 0.994 as a precision guard band, then descending RUS_o; if none qualify, use descending AUS as a feasibility recovery heuristic. Finalists are top 3 under strict fast AUS >= 0.995, top 3 under guard-band screening, the highest fast AUS candidate and equal mean (deduplicated; max 8). Confirm all finalists with original local score_model.py. Select from confirmed candidates only with exact AUS >= 0.995, maximizing RUS_o (Final tie-break). If none qualify, record no feasible winner and do not export models/S05.pt. Keep failed/infeasible results.

Existing verified soup reports contain four-model combinations (r017 or S03 plus the requested trio). Preserve these as historical comparisons, without changing the requested three-model search space.

- [x] RED tests for convex coefficient validation, weighted tensors, immutable inputs/frozen buffers, shape/dtype/finite guards, refinement, strict constrained selection, fast margin and exclusive save.
- [x] S05.py: pure search helpers, CPU float64 accumulation, strict ViT loading and isolated candidate evaluation child. Parent must never initialize CUDA.
- [x] tools/S05_run.py and configs/S05.yaml: checkpoint/source provenance, existing shared GPU lock and wait function, per-candidate subprocess/logs, progress/failure state, capped adaptive search and full validation shortlist.
- [x] GREEN CPU tests and real-checkpoint CPU schema/strict-load/endpoint/frozen-tensor verification. Test runner orchestration with fake scorers.
- [x] Register S05 in S_EXPERIMENTS.md and next ID S06. Launch detached suite, verify PID/state and record receipt. Existing S03/S04 and teammate files remain active.

All checkpoints/results use S05 IDs and exclusive creation. Every GPU operation holds tools.run_exp.LOCK and uses tools.run_exp.wait_for_gpu. No official/private submission. Cache fast evaluation differs numerically from original image decoding, so only the latter decides the final constraint.

S05 실행 등록: 2026-09-04T17:18:43.594944+00:00 (UTC), PID 305434. 접수 확인 상태: fast_or_waiting_for_gpu. 실시간 상태는 logs/S05.state.json.

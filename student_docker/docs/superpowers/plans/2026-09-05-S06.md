# S06 CKA Target-Floor implementation plan

1. Clone the r017 implementation into an isolated uppercase S06 source file.
2. Add a validated hinge helper implementing max(0, CKA-floor).
3. Match the r017 configuration exactly except for script/output paths and cka_floor=0.02.
4. Verify values, gradients, boundary behavior, invalid inputs, and configuration parity.
5. Run S06 through the repository GPU-locking runner and compare its validation result with r017.

The floor is applied to minibatch CKA during training. Validation CKA is a separate larger-sample measurement.
